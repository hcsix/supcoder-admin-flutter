import 'package:flutter/material.dart';

class RoleManageScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('角色管理'),
      ),
    );
  }
}
